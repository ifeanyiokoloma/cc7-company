import { TextField, Typography } from '@mui/material';
import React from 'react'

const RegisterUser = () => {
  return (
    <section>
          <Typography variant="h4" component="h1">Register User</Typography>
          <TextField />
    </section>
  );
}

export default RegisterUser